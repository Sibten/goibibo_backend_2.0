export enum bookingStatus {
  Upcoming,
  Completed,
  Closed,
}
export enum payment {
  Completed,
  Pending,
  Rejected,
  Refunded,
}

export enum flightStatus {
  Schduleded,
  Running,
  Completed,
}

export enum roles {
  SuperAdmin,
  Admin,
  User,
}

export enum gender {
  Male,
  Female,
  Other,
}


export enum Flightclass {
  Economy,
  PremiumEconomy,
  Business,
  FirstClass,
}