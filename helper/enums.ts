export enum BookingStatus {
  Upcoming,
  Completed,
  Closed,
}
export enum payment {
  Completed,
  Pending,
  Rejected,
  Refunded,
}

export enum FlightStatus {
  Schduleded,
  Running,
  Completed,
}

export enum roles {
  User, // 0
  Admin, // 1
  SuperAdmin, // 2
}

export enum gender {
  Male,
  Female,
  Other,
}

export enum Flightclass {
  Economy,
  PremiumEconomy,
  Business,
  FirstClass,
}

export enum AddOnType {
  Baggage,
  Insurance,
}
